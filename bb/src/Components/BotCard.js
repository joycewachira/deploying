import React, { useState } from 'react';
const BotCard = ({ bots, onEnlistBot, onReleaseBot }) => {
    // State to keep track of whether the bot is enlisted or not

   
  

    return (
        <div>
        </div>
    );
};

export default BotCard;
